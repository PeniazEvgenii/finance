spring:
  datasource:
    url: jdbc:postgresql://finance-db:5432/classifier
    username: postgres
    password: root
  liquibase:
    liquibase-schema: app               #пока в паблик схему ???

service:
  audit: audit-service:8080