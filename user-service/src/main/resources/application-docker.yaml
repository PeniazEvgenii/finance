spring:
  datasource:
    url: jdbc:postgresql://finance-db/user
    username: postgres
    password: root                       #может поменяется
#  liquibase:
#    liquibase-schema: app                # может в public закинуть